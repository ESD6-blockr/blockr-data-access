{
  "name": "blockr-data-access",
  "version": "1.0.0",
  "description": "Data access layer for Blockr",
  "main": "",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build-watch": "tsc -w -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint --project tsconfig.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ESD6-blockr/blockr-data-access.git"
  },
  "author": "Blockr",
  "license": "MIT",
  "dependencies": {
    "bluckur-models": "git+https://github.com/ESD6-blockr/blockr-models.git",
    "mongodb": "^3.1.13"
  },
  "devDependencies": {
    "tslint": "^5.14.0",
    "tsutils": "^3.9.1",
    "typescript": "^3.3.4000"
  }
}
